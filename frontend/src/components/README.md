# Components Directory

Reusable UI components for the WHERE TO STUDY application.

Planned components:
- `CafeCard` — Cafe preview card for list view
- `FilterPanel` — Facility filter controls
- `StarRating` — Star rating display/input
- `MapMarker` — Custom map marker
